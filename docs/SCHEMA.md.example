# Knowledge Base Schema

> 这是 Vault 的知识编译规则文档。Claude Code 执行 compile/lint 任务时必须先读取本文件。

## Vault 结构

| 目录 | 用途 |
|------|------|
| `raw/` | 原始资料，只增不删，不可修改 |
| `raw/web/` | 长文章型网页（Web Clipper Long-form Article 模板）|
| `raw/papers/` | 学术论文（Web Clipper arXiv Paper 模板）|
| `raw/data/` | 表格密集页面（fullHtml 抓取）|
| `raw/videos/` | YouTube 视频 metadata + 自动抓取的字幕 |
| `raw/wiki/` | Wikipedia 类条目 |
| `raw/code/` | GitHub Repo README |
| `raw/social/` | Twitter / 社交媒体 |
| `raw/conversations/` | 跟 Claude 的有价值对话归档 |
| `wiki/` | LLM 编译输出，结构化知识 |
| `wiki/concepts/` | 概念文章，每个概念一个 .md |
| `wiki/INDEX.md` | 全局目录，每次编译后更新 |
| `output/` | 查询结果、文章草稿、分析报告、lint 体检报告 |

## 编译规则（Compile Rules）

### 1. 处理 raw/ 文件时
- 识别文档中所有关键概念、人名、公司名、工具名
- 每个重要概念在 `wiki/concepts/` 创建或更新对应 .md
- 用 `[[wikilinks]]` 建立概念间连接
- frontmatter 必须包含：`source`、`compiled_at`、`related`

### 2. 概念文章结构
```yaml
---
title: 概念名称
source: [原始文件路径]
compiled_at: YYYY-MM-DD
related: [相关概念列表]
---

# 概念名称

## 核心定义
（一句话定义）

## 详细说明
（展开解释）

## 与其他概念的关系
（[[wikilinks]] 连接）

## 来源摘录
（关键引语）
```

### 3. INDEX.md 维护规则
- 每次 compile 后必须更新 INDEX.md
- 每条格式：`[[文件名]] — 一句话摘要（来源：raw/xxx）`
- 按主题分类排列

### 4. Lint 规则（健康检查）
执行 lint 时检查：
- 有无孤立概念（没有任何 wikilink 指向它）
- 有无内容矛盾
- 有无明显缺失的关联概念
- 哪些 raw/ 文件还未被编译
- 哪些 wiki/concepts/ 已 90 天未更新但 raw 已变更

## 优先主题（自定义）

> 这一节是给你 LLM 的提示：我个人最关心哪些主题，编译时这些主题的 concept 优先级更高、内容更详细。
> 替换成你自己的优先级清单：

1. **示例主题 1**：xxx
2. **示例主题 2**：xxx
3. **示例主题 3**：xxx

## Compile Prompt 模板

当你需要让 Claude 编译 raw/ 目录中的新文件时：

---
**[COMPILE PROMPT]**

请读取 `wiki/SCHEMA.md` 了解编译规则，然后编译以下新增的 raw 文件：

`raw/[目录]/[文件名].md`

执行步骤：
1. 识别文档中所有关键概念
2. 在 `wiki/concepts/` 为每个关键概念创建/更新 .md 文件
3. 建立 `[[wikilinks]]` 交叉引用
4. 更新 `wiki/INDEX.md`
---

## Query Prompt 模板

---
**[QUERY PROMPT]**

请先读取 `wiki/INDEX.md` 了解知识库全貌，然后回答：

问题：[你的问题]

如果需要深入，再读取相关 `wiki/concepts/` 文件。
将本次查询结果保存到 `output/[主题]-[日期].md`。
---

## Lint Prompt 模板

---
**[LINT PROMPT]**

请扫描整个 `wiki/` 目录，执行健康检查：
1. 列出孤立概念（无 wikilink 指向）
2. 发现潜在内容矛盾
3. 建议应该建立但还没有的连接
4. 列出 raw/ 中未被编译的文件
5. 找出 wiki/concepts/ 里 compiled_at 超过 90 天且引用的 raw 文件已更新的 concepts

将报告保存到 `output/lint-[日期].md`。
---
